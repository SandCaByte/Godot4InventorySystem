extends Node

#Global Variables
var item_held = null
var default_color := Color(Color.WHITE, 0.2)
var taken_color := Color(Color.RED, 0.2)
var stackable_color := Color(Color.BLUE, 0.2)
var free_color := Color(Color.GREEN, 0.2)

# Called when the node enters the scene tree for the first time.
func _ready():
	pass # Replace with function body.


# Called every frame. 'delta' is the elapsed time since the previous frame.
func _process(delta):
	pass
